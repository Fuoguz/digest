> 历史记录：以下是 2026-09-18 的阻塞状态，已由 2026-09-19 的 [0.2.1 真实模型验收](V0.2.1_REAL_MODEL_ACCEPTANCE.md) 取代。当前权限已恢复，勿将下文未验收状态当作当前结论。

> 补充权限核查：Vercel connector 列出团队为空；请求仓库关联项目返回 403 Forbidden。当前无法访问该生产项目。

# 2026-09-18 验收状态：外部权限阻塞，未调用真实模型

本轮 Vercel CLI `env ls production --cwd .pilot-deploy` 返回无有效凭据并进入登录流程；已停止等待，未读取/输出秘密。当前进程无 MODEL_API_KEY。没有可合法使用的生产会话，未绕过试用码认证。

本机 Node 对已记录的生产地址 digest-sigma.vercel.app 的 HTTP 检查发生连接错误，没有得到状态码；不能据此断言部署损坏或可用。没有执行本轮上线。

因此 General / Academic Paper / Legal Case / Policy Document 的真实模型 schema、中文/英文/长文质量、claim-evidence 语义支持、review question 可答性与 relation suggestion 质量 **仍未验证**。新 Course Feedback 的具体性、rubric 遵循与语义可靠性也未验证。

离线 schema/引用/取消/竞争测试和 Chrome mock UI 验收已经执行，但不计入真实模型验收。当前不可向 5 名学生承诺已验证的免配置生产服务。

恢复访问后，按下方四模式有限流程执行，并额外对一份学生真实 Task/Answer 请求一次 Feedback，人工核查每条 gap 与引文是否支持修改建议；对同样材料交给学生修订后，数日后用不同案例重新作答。不得用引文匹配率代替语义质量。

---

以下为历史记录与有限验收步骤：
# 真实模型验收

**真实模型验收待执行**（2026-09-08）。

本轮通过正常开发入口的配置 UI 检查，Key 输入框显示“填写你的 Key”，未发现用户已配置的可用服务。没有读取、打印或导出 Key，没有真实模型调用。开发没有因此阻塞。

自动测试与浏览器 QA 使用显式 mock fixture。mock 的有效引用、无关资料不连边、关系理由仅证明流程与验证器工作，不证明真实模型稳定性、语义质量或正确率。

## 后续有限验收步骤

1. 用户在 Developer AI Service 配置可合法使用的 endpoint/model/key；不将 Key 写入 fixture、截图、日志或 Git。
2. 各导入一份 General 中文课程资料、Academic Paper 文本 PDF、Legal Case、Policy Document。记录来源、字符/段落数与模式，保持原文不变。
3. 每种模式先请求一次，记录成功/repair/失败、耗时、完整结构、missing 是否合理、总 Claim 数与 matched/invalid/ambiguous 数。
4. 逐条核查 quote 是原文逐字引用，点击验证段落及页码；推论与资料陈述区分。重复引文应 ambiguous，不改写原文来通过测试。
5. 选择长文复测一次；若超过服务输入限制，如实记录限制、截断或失败，不宣称完整研读。核查失败保留旧结果。
6. 从两份相关资料确认知识点，请求关系建议，人工核查理由和来源；加入无关资料，接受空建议，拒绝牵强关系。
7. 每个模式最多一次必要重试，避免无限请求和费用；输出不含秘密的验收记录。

建议记录表：模式、模型、原文长度、成功/修复次数、missing 判断、matched/总判断、invalid/ambiguous、人工核查结论、限制。Evidence matched 仅表示原文可定位，不代表推论真实。
